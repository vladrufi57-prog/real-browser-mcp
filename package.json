{
  "name": "real-browser-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A real-browser MCP server and Chromium extension for controlling an already open, logged-in browser.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "dev": "node --loader ts-node/esm src/server/index.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.18.0",
    "ws": "^8.18.0",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/ws": "^8.18.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  }
}
